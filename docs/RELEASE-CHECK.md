# v0.1 本地发布检查

更新日期：2026-09-23。发布状态：**本地打包与安装检查通过；两轮不同目标的真实评测已完成；尚未上传 GitHub。**

**最新人物模拟评测：两组都要求模拟泡泡老师，仅一组加载Skill。两名Judge分别在10/12、12/12题偏好Skill，四维均分3.46对2.66，观察到还原度增益。** [报告与全部案例](../paopao-perspective-skill/tests/persona-ab-v1/run/EVAL-REPORT.md)。尚非高保真认证，历史 rc2 ZIP 不作为当前发行包；发布时从当前源码重新构建。

历史普通顾问baseline评测：ON 0/12，六维2.806对3.792，[旧报告](../paopao-perspective-skill/tests/release-v01/token8192-run/EVAL-REPORT.md)。它检验通用建议质量，不应作为人物忠实度主结论；两轮题目和标准不同。更早的审批/TLS/截断记录继续保留，下文关于阻塞均为历史状态。

## 2026-09-23 发布收尾验收

在独立公开源码副本执行，未复制工作区密钥、字幕或 RAG 索引。使用 Node.js 24.19.0、pnpm 11.19.0；浏览器验收使用本机 Chrome、隔离记录和模拟模型响应。

- 锁文件独立安装：通过。
- 类型检查、22 项单元与接口测试、生产构建：通过。
- 4 项公开包工具测试：通过。
- 8 项生产版浏览器测试：通过，包含小结更新的取消、Esc、确认生成与刷新恢复。
- 修复 Esc 取消小结更新同时关闭侧栏的问题；删除持久化测试等待删除完成再刷新。
- 小结更新改用页面内确认，旧小结仍保留在会话导出数据中。
- 固定 pnpm 11.19.0，声明 Node.js 24+，新增 GitHub 自动检查并纳入公开包。
- 本次没有执行新的付费模型评测。公开副本的无索引回退由接口测试覆盖，浏览器回复为模拟数据。
- GitHub Actions 已配置，远程执行状态待首次推送后确认；尚未上传 GitHub。

以下保留此前研究与发布记录，历史结果不代表本次重新执行。

## 本轮改变

- 重写 Skill 与应用 README，加入真实回答节选、用途、安装、阅读路线与已知状态。
- 添加自包含标准库安装器，默认拒绝覆盖已有安装。
- 添加原创部分 MIT 与 Nuwa 上游完整通知，区分第三方语料权利。
- 新增公开包构建与检查工具，保留来源卡片和净化索引，排除完整字幕/OCR、密钥和本地头像。
- 将无提交的嵌套 Skill Git 元数据迁入可恢复本地归档，采用外层仓库统一管理。
- 冻结 12 道新配对题，其中 3 道双轮纠正，保存执行器和失败记录。

人物运行文件 SKILL.md 未修改，哈希仍是 `5236676eb916fc74bc81de04d1569a3bc593d53ccd37c7954882f30d24a0f25c`。

## 已验证

| 项目 | 结果 | 范围 |
|---|---|---|
| Skill 结构与本地链接 | 通过 | 原目录与轻量包安装副本 |
| 打包回归 | 4 项通过 | 排除、净化、链接变换、哈希与安全输出 |
| 应用核心测试 | 5 项通过 | 本地与无完整字幕的公开源码副本 |
| TypeScript | 通过 | 本地及公开源码副本 |
| 生产构建 | 通过 | 临时公开源码副本，复用本机已安装依赖，无 API key |
| 安装器 | 通过 | 实际 ZIP 解压并安装到临时目录，重复安装返回错误且不覆盖 |
| 发布检查 | 通过 | 本地链接、秘密模式、禁止文件、来源索引、运行资产及 MANIFEST |
| 独立读者审查 | 无 P0/P1；1 项 P2 已修正 | 轻量包不再附带不可独立执行的应用维护说明 |

这些检查不等于所有客户端自动发现、全新机器依赖下载、真实 DeepSeek 效果或公网部署均已验证。没有在这一轮重新运行浏览器交互模拟测试。

## 真实模型测试为什么未完成

沙箱内 24 个首轮请求均连接失败，没有有效模型回答。升级联网执行被自动审批拒绝，原因是尚未明确授权把本地 Skill、领域框架与新测试内容外发到 DeepSeek。未绕过拒绝，没有执行 Judge，也没有报告胜率。

下一步拟发送：当前 Skill、Expression DNA、六份领域框架、12 道虚构测试及各自对话历史。不会发送原始视频、完整语料库、真实个人简历或浏览器记录。密钥仅用于向 DeepSeek 认证，不写入测试文件。完整成功运行预计 30 次请求，使用现有账户产生 API 费用。

输入、脚本、参数与空结果状态见 [评测记录](../paopao-perspective-skill/tests/release-v01/README.md)。取得外发授权后，以新的输出目录运行；成功后再进行独立匿名评分。

## 可交付物

两份 ZIP 和解包源码通过 `scripts/build_release.py` 生成，每包附文件清单及哈希。完整本地研究仍保留。GitHub 创建、Git 提交、推送与托管均未执行。
